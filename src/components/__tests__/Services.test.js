import { render } from '@testing-library/react';
import Services from '../Services';

describe('Services test', () => {
  it('Rendering the Services component', () => {
    render(<Services />);
  });
});
